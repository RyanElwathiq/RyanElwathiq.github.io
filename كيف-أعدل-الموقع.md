# 📖 دليل ريّان — كيف تعدّل موقعك بنفسك

> كل شغلك بيكون بملفات معدودة. هاد الدليل بيوريك وين كل إشي.

---

## ١) إضافة عمل جديد (فيديو / تصميم / موقع / تسويق)

افتح الملف: **`src/data/work.json`**

انسخ أي مشروع موجود (من `{` لـ `}`)، الصقه، وعدّل:

| الحقل | شو بتحط فيه |
|---|---|
| `id` | اسم مختصر بالإنجليزي بدون مسافات — **هذا بيصير رابط صفحته** (`/work/الـ-id/`) |
| `title` / `titleAr` | اسم المشروع بالإنجليزي والعربي |
| `role` / `roleAr` | شو كان دورك فيه |
| `category` | `video` أو `design` أو `website` أو `marketing` |
| `order` | رقم الترتيب — **أول ٤ بيظهروا بالهوم والباقي بصفحة القسم تلقائياً** |
| `youtube` | 👈 رقم الفيديو من اليوتيوب (اللي بعد `youtu.be/`) |
| `cover` | صورة الغلاف — حط الصورة بمجلد `public/assets` واكتب `/assets/اسمها.png` |
| `desc` / `descAr` | وصف قصير باللغتين |
| `url` | رابط الموقع (لمشاريع المواقع فقط) |
| `status` | للمواقع فقط: `live` (شغّال) أو `wip` (تحت الشغل) |

### 🔑 وين بلاقي رقم اليوتيوب؟
`https://youtu.be/Kp7eSUU9oy8` ← الرقم هو `Kp7eSUU9oy8`
`https://youtube.com/watch?v=Kp7eSUU9oy8` ← نفس الإشي، اللي بعد `v=`

> ⚙️ **بدك تغيّر كم عمل يظهر بالهوم؟** افتح `src/pages/index.astro` وغيّر `HOME_LIMIT = 4`

---

## ٢) كتابة قصة المشروع (صفحته المستقلة)

كل مشروع إله **صفحة خاصة بتتولد لحالها** على `/work/الـ-id/` — ما بتحتاج تعمل ملف جديد أبداً.

عشان تعبّي القصة، ضيف جوا المشروع بـ `work.json`:

```jsonc
"story": {
  "vision":    "الرؤية بالإنجليزي",
  "goal":      "الهدف",
  "challenge": "التحدي",
  "approach":  "المقاربة",
  "result":    "النتيجة"
},
"storyAr": {
  "vision":    "الرؤية بالعربي",
  "goal":      "الهدف",
  "challenge": "التحدي",
  "approach":  "المقاربة",
  "result":    "النتيجة"
}
```

> ✅ **أي حقل بتشيله أو بتتركه فاضي — قسمه ما بيظهر بالصفحة أصلاً.** فما في خوف تعبّي شوي وتكمّل بعدين.
>
> 📌 **شوف مثال كامل معبّى** بمشروع `plasmajo` بنفس الملف — انسخ شكله.

---

## ٣) كتابة مقال بالمدونة

اعمل ملف جديد بمجلد **`src/content/blog/`** اسمه مثلاً `ar-عنوان-المقال.md`، وانسخ فيه:

```markdown
---
title: 'عنوان المقال'
description: 'وصف قصير — هذا اللي بيظهر بجوجل، خليه مغري'
date: 2026-08-01
lang: 'ar'
tags: ['وسم', 'وسم ثاني']
draft: false
---

هون بتكتب المقال عادي.

## عنوان فرعي

نص، **كلمة غامقة**، و[رابط](https://example.com).
```

- `lang: 'ar'` بيحط المقال بالمدونة العربية، و `'en'` بالإنجليزية
- `draft: true` = مسودة، ما بتنشر
- الرابط بيصير: `/ar/blog/اسم-الملف/`

---

## ٤) تعديل النصوص والمقاسات

**كل** ملف قسم فيه:
- قسم **«النصوص (عدّل من هون)»** بأول الملف — كل الجمل بالعربي والإنجليزي
- صندوق **`🎛️ لوحة التحكم`** بأول الـ `<style>` — كل رقم مشروح جنبه

### وين ملف كل قسم

| القسم بالموقع | الملف |
|---|---|
| الشاشة الأولى (الهيرو + الفيديو) | `src/components/Hero.astro` |
| شريط الأرقام | `src/components/Stats.astro` |
| «من أنا» + الفيديو الرمزي | `src/components/About.astro` |
| شريط الفيديوهات (السحب الأفقي) | `src/components/WorkStrip.astro` |
| بطاقة أي عمل | `src/components/WorkCard.astro` |
| قصص النمو | `src/components/CaseRows.astro` |
| التصاميم + بلاطة Behance | `src/components/DesignsGrid.astro` |
| المواقع + الفيديو + القائمة | `src/components/Websites.astro` |
| المهارات + شريط الأدوات | `src/components/Skills.astro` |
| **كيف بشتغل / الأسعار / ليش أنا** | `src/components/Pricing.astro` |
| الدراسة | `src/components/Education.astro` |
| تواصل معي | `src/components/Contact.astro` |
| شريط التنقل فوق | `src/components/Nav.astro` |
| الفوتر | `src/components/Footer.astro` |
| نافذة تشغيل الفيديو | `src/components/Lightbox.astro` |
| صفحة المشروع الواحد | `src/layouts/ProjectPage.astro` |
| صفحات الأقسام (كل الفيديوهات...) | `src/layouts/WorkIndex.astro` |
| قائمة المقالات / المقال | `src/layouts/BlogIndex.astro` / `BlogPost.astro` |

### بدك تغيّر ترتيب الأقسام؟
افتح `src/pages/index.astro` (إنجليزي) أو `src/pages/ar/index.astro` (عربي) — كل قسم سطر واحد، حرّكه فوق أو تحت.

---

## ٥) تغيير الألوان (بكل الموقع مرة وحدة)

افتح: **`src/styles/tokens.css`** — غيّر `--accent` وكل الموقع بيتغير.

---

## ٦) الفيديوهات المتحركة مع السكرول

بالموقع **٣ فيديوهات** بتتحرك مع سكرولك:

| الفيديو | مجلد فريماته |
|---|---|
| الهيرو (ديسكتوب) | `public/frames/desktop/` |
| الهيرو (موبايل) | `public/frames/mobile/` |
| «من أنا» | `public/frames/about/` |
| المواقع | `public/frames/websites/` |

**بدك تغيّر فيديو؟** قوللي وأنا بولّد واحد جديد وبفكفكه وبركّبه — العملية آلية عندي.

**إعدادات الحركة:** أول ملف `src/scripts/sequence.js` (طول الرحلة، توقيت النصوص) و `src/scripts/motion.js` (نعومة السكرول، سرعة العدادات).

---

## ٧) كيف تشوف تعديلاتك قبل النشر

افتح PowerShell بمجلد `D:\Ryan-Portfolio\site` واكتب:

```
npm run dev
```

وافتح بالمتصفح: `http://localhost:4321` — أي حفظ بيبين فوراً.

> ⚠️ لو التعديل ما بان، سكّر السيرفر (`Ctrl+C`) وشغّله من جديد — أحياناً بيعلق على نسخة قديمة.

---

## ٨) كيف تنشر التعديلات عالموقع الحقيقي

```
git add -A
git commit -m "وصف قصير للتعديل"
git push
```

> 💡 أو ببساطة: قول لكلود «انشر التعديلات» وهو بيعملها 😄
