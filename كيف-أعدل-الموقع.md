# 📖 دليل ريّان — كيف تعدّل موقعك بنفسك

> كل شغلك بيكون بملفات معدودة. هاد الدليل بيوريك وين كل إشي.

---

## ١) إضافة عمل جديد (فيديو / تصميم / موقع)

افتح الملف: **`src/data/work.json`**

انسخ أي مشروع موجود (من `{` لـ `}`)، الصقه، وعدّل:

| الحقل | شو بتحط فيه |
|---|---|
| `id` | اسم مختصر بالإنجليزي بدون مسافات (مثال: `"my-new-video"`) |
| `title` / `titleAr` | اسم المشروع بالإنجليزي والعربي |
| `role` / `roleAr` | شو كان دورك فيه |
| `category` | `video` أو `design` أو `website` أو `marketing` |
| `featured` | `true` = بيظهر بالصفحة الرئيسية، `false` = بصفحة القسم بس |
| `order` | رقم الترتيب (الأصغر بيظهر أول) |
| `youtube` | 👈 **رقم الفيديو من اليوتيوب** (اللي بعد `youtu.be/`) |
| `cover` | صورة الغلاف — حط الصورة بمجلد `public/assets` واكتب `/assets/اسمها.png` |
| `desc` / `descAr` | وصف قصير باللغتين |

### 🔑 وين بلاقي رقم اليوتيوب؟
الرابط: `https://youtu.be/Kp7eSUU9oy8` ← الرقم هو `Kp7eSUU9oy8`
أو: `https://youtube.com/watch?v=Kp7eSUU9oy8` ← نفس الإشي، اللي بعد `v=`

---

## ٢) تعديل نصوص الهيرو (أول شاشة)

افتح: **`src/components/Hero.astro`**

فوق بالملف في قسم مكتوب عليه **«النصوص (عدّل من هون)»** — كل جملة واضح مكانها بالعربي والإنجليزي.

---

## ٣) تعديل المقاسات والتباعد (حجم الخط، المسافات بين السطور...)

نفس الملف **`src/components/Hero.astro`** — انزل لقسم `<style>` ورح تلاقي:

```
🎛️ لوحة التحكم — عدّل من هون
```

كل رقم مشروح جنبه بالعربي: حجم العنوان، المسافة بين السطور، تباعد الحروف، المسافات العمودية... غيّر الرقم واحفظ.

---

## ٤) تغيير الألوان (بكل الموقع مرة وحدة)

افتح: **`src/styles/tokens.css`** — غيّر اللون من عند `--accent` مثلاً وكل الموقع بيتغير.

---

## ٥) سرعة وطول أنيميشن السكرول

افتح: **`src/scripts/sequence.js`** — أول الملف في لوحة تحكم:
- `SCROLL_LENGTH`: قديش الهيرو بيضل مثبت وانت بتسكرول
- `INTRO_FADE_END`: إيمتى بيختفي العنوان
- `OUTRO_START`: إيمتى بتظهر الجملة الختامية

وبملف **`src/scripts/motion.js`**: `SMOOTH_DURATION` = نعومة السكرول.

---

## ٦) كيف تشوف تعديلاتك قبل النشر

افتح PowerShell بمجلد `D:\Ryan-Portfolio\site` واكتب:

```
npm run dev
```

وافتح بالمتصفح: `http://localhost:4321` — أي حفظ بيبين فوراً.

---

## ٧) كيف تنشر التعديلات عالموقع الحقيقي

اكتب بالـ PowerShell (بنفس المجلد):

```
git add -A
git commit -m "وصف قصير للتعديل"
git push
```

وخلال دقيقتين بتكون التعديلات حية على ryanalali.me

> 💡 أو ببساطة: قول لكلود «انشر التعديلات» وهو بيعملها 😄
