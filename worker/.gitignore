node_modules/
.wrangler/
# ⚠️ ملفات المتغيّرات المحلية — ممنوع ترفع، بتحتوي مفاتيح
.dev.vars
.env
# ملفات مؤقتة لتمرير المفاتيح لـ wrangler secret put --file
# (احذفها بعد الاستخدام — هاد سطر أمان مش بديل عن الحذف)
token.txt
*.secret
*.key
